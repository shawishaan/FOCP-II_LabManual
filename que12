#include<iostream>
using namespace std;
int main()
{
    int a,b,c;
    cout<<" enter the sides of triangle";
    cin>>a>>b>>c;
    if(a==b &&b==c){
        cout<<"equilateral";
    }
    else if(a==b||b==c||a==c){
        cout<<" isoscles";
    }else{
        cout<<"scalene";
    }
    return 0;
}